import React from "react";

import styles from "./LinkBudget.module.scss";

const LinkBudgetPage = () => {
  return (
    <div className={styles.Container}>
      Could present options that would take to an online excel web app, excel
      like or a full blown Link Budget Web App (drag/drop/build)
    </div>
  );
};

export default LinkBudgetPage;
